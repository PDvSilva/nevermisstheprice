import ModernHome from "../ModernHome";
import { ThemeProvider } from "@/components/ThemeProvider";

export default function ModernHomeExample() {
  return (
    <ThemeProvider>
      <ModernHome />
    </ThemeProvider>
  );
}
